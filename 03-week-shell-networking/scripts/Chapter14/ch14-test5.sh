#!/bin/bash
# Testing the $0 parameter

# In Bash, the $0 parameter represents the name of the script or shell that is currently running. 
#It is a special shell variable that stores the first word of the command line that was used to invoke the current shell or script.

echo The zero parameter is set to: $0